# =============================================================================
#  Python 学习速查手册
#  配套：python.md（讲原理 · 表格速查）  ·  本文件（查写法 · 示例代码）
#  用法：本文件整体保持注释状态；想运行哪段代码，取消对应行的 # 即可
# =============================================================================

#  目录
#    01  变量与基本输出
#    02  数据类型与查看 type()
#    03  字符串基础（定义 · 转义 · 拼接 · 重复）
#    04  字符串格式化（% 占位符 · f-string）
#    05  输入 input()
#    06  运算符（算术 · 赋值 · 比较 · 逻辑）
#    07  条件判断 if / if...else
#    08  分支结构 if...elif...else / match...case
#    09  综合练习
#    10  循环 while
#    11  循环 for 与 range()
#    12  嵌套循环
#    13  break 与 continue
#    14  random 模块与 while True
#    15  循环综合练习
#    16  字符串进阶（索引 · 切片 · 常用方法）
#    17  数据容器：列表 list
#    18  字符串综合案例

# =============================================================================


# =============================================================================
#  01  变量与基本输出
# =============================================================================

# --- 1.1  最简单的输出
# print() 把内容打印到控制台，字符串要用引号括起来。

# print("Hello, World!")

# --- 1.2  布尔值也能直接打印
# True / False 是布尔值，本质就是数字 1 / 0。

# num = True
# print(num)

# --- 1.3  变量累加
# 变量可以反复赋值；等号右边先算，再存回左边的变量。

# start = 10.7
# add = 5
# num = start + add
# print("第一个月", num)
# num = num + add
# print("第二个月", num)

# --- 1.4  交换两个变量的值
# 借第三个临时变量 temp 中转，是理解赋值顺序的经典例子。

# a = 100
# b = 200
# c = 300
# temp_num = c      # 先把 c 存起来
# c = a
# a = b
# b = temp_num      # 再把存好的值放回 b
# print(c, a, b)

# --- 1.5  print() 的 end 参数
# print() 默认在结尾补一个换行；用 end="" 可以去掉这个换行，
# 让多次 print 的输出接在同一行 —— 画图形时经常用（见第 12 章）。

# print("a", end="")     # 结尾不加换行
# print("b", end="")
# print("c")             # 合起来输出：abc


# =============================================================================
#  02  数据类型与查看 type()
# =============================================================================

# --- 2.1  type() 查看数据类型
# 常见类型：int 整数 / float 小数 / bool 布尔 / str 字符串 / NoneType 空值。

# print(type(b))        # <class 'int'>
# print(type(True))     # <class 'bool'>
# print(type(False))    # <class 'bool'>
# print(type(3.14))     # <class 'float'>
# print(type(10))       # <class 'int'>
# print(type(None))     # <class 'NoneType'>


# =============================================================================
#  03  字符串基础（定义 · 转义 · 拼接 · 重复）
# =============================================================================

# --- 3.1  多行字符串
# 用三个双引号包裹，可以跨行书写，换行和缩进都会原样保留。

# print("""
# 多行字符串
# "这是一本很牛逼的书"
# 多行字符串就是可以写多行字符串
# 杀杀杀   sss
#      水水水水水
# """)

# --- 3.2  转义字符 \
# \ 用来表示特殊字符，比如 \' 表示单引号本身。
# 常见转义：\' 单引号  ·  \" 双引号  ·  \n 换行  ·  \t 制表符

# print('it\'s very nb')

# --- 3.3  字符串拼接（用 + 号）
# + 号只能拼接字符串；数字要先 str() 转成字符串再拼。

# name = "涛哥"
# age = 18
# pro = "软件工程"
# like = "python" + "java"
# print("我叫" + name + "今年" + str(age) + "岁，我学的专业是" + pro + "，我的爱好是" + like)

# --- 3.4  字符串重复（用 * 号）
# 字符串乘以一个整数，就是把它重复几遍；配合第 12 章画图形可以少写一层循环。

# print("*" * 5)        # *****
# print("-" * 10)       # ----------


# =============================================================================
#  04  字符串格式化（% 占位符 · f-string）
# =============================================================================

# --- 4.1  % 占位符
# %s 代表一个字符串占位，后面用 % 接上要填入的值。

# 单变量
# S1 = "小牛子"
# print("我叫%s" % S1)

# 多变量：值用括号包起来，按顺序一一对应
# S2 = "18"
# print("我叫%s,今年%s" % (S1, S2))

# --- 4.2  f-string（推荐写法）
# 在字符串前加 f，用 {} 直接嵌入变量；可读性最好，也不用 str() 转换。

# S1 = "小牛子"
# S2 = "18"
# print(f"我叫{S1},今年{S2}岁")


# =============================================================================
#  05  输入 input()
# =============================================================================

# --- 5.1  input() 基础
# input() 会让程序暂停，等你输入后回车；**输入的内容永远是字符串类型**。

# name = input("请输入你的姓名")
# print(f"你好！{name}")

# --- 5.2  输入数字要转换类型
# 要做数学运算，必须先用 int() / float() 把字符串转成数字。

# num = 10000
# password = input("请输入您的密码")
# num_low = int(input("请输入您要取出的金额"))
# print(f"您的余额已取出{num_low}元，剩余{num - num_low}元")


# =============================================================================
#  06  运算符（算术 · 赋值 · 比较 · 逻辑）
# =============================================================================
# 完整的运算符表见 python.md 第六章，这里放练习示例。

# --- 6.1  加法计算器
# float() 转成小数，支持小数运算；f-string 的 {} 里可以直接写表达式。

# print("欢迎使用加法计算器")
# num1 = float(input("请输入第一个数字"))
# num2 = float(input("请输入第二个数字"))
# print(f"{num1} + {num2}等于{float(num1 + num2)}")

# --- 6.2  加减运算
# input1 = float(input("请输入第一个数字"))
# input2 = float(input("请输入第二个数字"))
# print(f"加法运算的结果是{input1 + input2}")
# print(f"减法运算的结果是{input1 - input2}")

# --- 6.3  逻辑运算 and / or
# and：两边都为真才为真；  or：有一边为真就为真。

# num = int(input("请输入一个数字"))
# print(f"您输入的数字{num >= 10 and num <= 20}")
# print(f"您输入的数字{num >= 10 or num <= 20}")


# =============================================================================
#  07  条件判断 if / if...else
# =============================================================================

# --- 7.1  if 单分支
# 条件为 True 才执行缩进的代码块；冒号和缩进是 Python 的硬性要求。

# score = 600
# if score >= 600:
#     print("恭喜你考上了家里蹲")

# --- 7.2  if 多条件判断（登录示例）
# 用 == 判断相等，and / or 组合多个条件。

# ok_account = "13111513523@163.com"
# ok_password = '123456'
# account = input("请输入您的账号")
# password = input("请输入您的密码")
# if account == ok_account and password == ok_password:
#     print("登录成功")
# if account != ok_account or password != ok_password:
#     print("登陆失败：账号或密码错误")

# --- 7.3  if...else 二选一
# 条件成立走 if，否则走 else，比上面写两个 if 更清晰。

# ok_account = "13111513523@163.com"
# ok_password = '123456'
# account = input("请输入您的账号")
# password = input("请输入您的密码")
# if account == ok_account and password == ok_password:
#     print("登录成功")
# else:
#     print("登陆失败：账号或密码错误")

# --- 7.4  练习：判断闰年
# 规则：能被 4 整除且不能被 100 整除，或者能被 400 整除。

# year = int(input("请输入年份"))
# if (year % 4 == 0 and year % 100 != 0) or year % 400 == 0:
#     print(f"{year}是闰年")
# else:
#     print(f"{year}是平年")

# --- 7.5  练习：判断奇偶
# num % 2 == 0 说明能被 2 整除，是偶数。

# num = int(input("请输入一个数字"))
# if num % 2 == 0:
#     print(f"你输入的数字是偶数")
# else:
#     print(f"你输入的数字是奇数")


# =============================================================================
#  08  分支结构 if...elif...else / match...case
# =============================================================================

# --- 8.1  if...elif...else 语法骨架
# 多条件从上往下依次判断，命中一个就跳出，最后 else 兜底。

# if 要判断的条件1:
#     执行的代码1
# elif 要判断的条件2:
#     执行的代码2
# else:
#     条件都不成立时执行的代码3

# --- 8.2  match...case 语法骨架
# Python 3.10+ 的多分支写法，比一长串 elif 更直观；case _ 相当于 else。

# match 要判断的条件:
#     case 条件1:
#         执行的代码1
#     case 条件2:
#         执行的代码2
#     case _:
#         条件都不匹配时执行的代码3

# --- 8.3  用 match...case 写计算器
# num1 = float(input("请输入第一个数字"))
# num2 = float(input("请输入第二个数字"))
# oper = input("请输入运算符（+、-、*、/）")
# match oper:
#     case "+":
#         print(f"{num1} + {num2} = {num1 + num2}")
#     case "-":
#         print(f"{num1} - {num2} = {num1 - num2}")
#     case "*":
#         print(f"{num1} * {num2} = {num1 * num2}")
#     case "/":
#         print(f"{num1} / {num2} = {num1 / num2}")
#     case _:
#         print("输入的字符有误，请检查后重新输入")

# --- 8.4  案例：游戏角色控制系统
# 用 match 把按键映射成坐标变化；.lower() 让输入不区分大小写。
# （本段原为可运行代码，为保持手册统一已注释；想跑，取消下方每行开头的 #）

# print("欢迎使用游戏角色控制系统")
# print("请开始操作您的角色")
# x = 0
# y = 0
# key = input("请输入角色的移动方向（w上、s下、a左、d右）").lower()
# match key:
#     case "w":
#         y += 1
#         print(f"角色移动到坐标点({x},{y})")
#     case "s":
#         y -= 1
#         print(f"角色移动到坐标点({x},{y})")
#     case "a":
#         x -= 1
#         print(f"角色移动到坐标点({x},{y})")
#     case "d":
#         x += 1
#         print(f"角色移动到坐标点({x},{y})")
#     case _:
#         print("输入的字符有误，请检查后重新输入")


# =============================================================================
#  09  综合练习
# =============================================================================

# --- 9.1  会员准入与取款
# vip = int(input("请输入您的会员等级0-1"))
# token = float(input("请输入您的积分"))
# print(f"{vip >= 1 and token <= 2000}")
# balance = 10000
# num = float(input("请输入要取出的金额"))
# print(f"您已取出{num}元钱，剩余{balance - num}元钱")


# =============================================================================
#  10  循环 while
# =============================================================================

# --- 10.1  while 基本语法
# 只要条件为 True，就重复执行缩进的代码块；条件变 False 时循环结束。
# 循环三要素，缺一不可：
#   ① 循环前：初始化变量    ② while 后：判断条件    ③ 循环体内：更新变量

# while 条件:
#     重复执行的代码
#     更新变量

# --- 10.2  案例：一百以内所有偶数的累加
# 从 0 数到 100，能被 2 整除（也就是偶数）就累加进 total。

# total = 0
# num = 0
# while num <= 100:
#     if num % 2 == 0:
#         total += num
#     num += 1
# print(f"一百以内所有偶数的累加结果是{total}")


# =============================================================================
#  11  循环 for 与 range()
# =============================================================================

# --- 11.1  for 基本语法
# for 会把序列里的元素一个一个取出来，每取一个就执行一遍缩进的代码块。

# for 变量 in 序列:
#     重复执行的代码

# --- 11.2  for 遍历字符串
# 字符串也是序列，for 会一个字符一个字符地取出，逐个赋给 i。

# msg = input("请输入一段字符串：")
# for i in msg:
#     print(i)
# print("遍历结束")        # 没有缩进，所以循环全部跑完才执行这一句

# --- 11.3  range() 的三种用法
# range() 生成一段整数序列，专门配合 for 使用；生成的数字都不含结尾那个数。
#   ① range(end)                从 0 到 end-1
#   ② range(start, end)         从 start 到 end-1
#   ③ range(start, end, step)   从 start 到 end-1，每次跳 step 步

# for i in range(5):          # 0 1 2 3 4
#     print(i)
# for i in range(2, 8):       # 2 3 4 5 6 7
#     print(i)
# for i in range(1, 10, 2):   # 1 3 5 7 9
#     print(i)

# --- 11.4  案例：for 循环累加一百以内的奇数
# 与上面的偶数累加对照着看：同样数 0~100，这次只留下不能被 2 整除的（奇数）。

# total = 0
# for num in range(101):      # range(101) 刚好覆盖 0~100
#     if num % 2 == 1:
#         total += num
# print(f"一百以内所有奇数的累加结果是{total}")

# --- 11.5  案例：100-500 之间 3 的倍数之和
# 同一个累加套路，只换了区间和判断条件。

# total = 0
# for num in range(100, 501):     # 写 501 是为了把 500 也含进来（range 不含结尾）
#     if num % 3 == 0:
#         total += num
# print(f"100-500 之间所有 3 的倍数之和是{total}")


# =============================================================================
#  12  嵌套循环
# =============================================================================

# --- 12.1  什么是嵌套循环
# 一个循环里再套一个循环，叫嵌套循环。
# 执行顺序：外层转一圈，内层就要完整转一圈。
# 总的执行次数 = 外层次数 × 内层次数。
#
# 典型用途：打印有行有列的图形 —— 外层控制「行」，内层控制「这一行里的每一列」。

# for i in range(3):          # 外层：3 行
#     for k in range(4):      # 内层：每行 4 个
#         print("*", end="")
#     print()                 # 一行打完，换行（这句在外层，不在内层）

# --- 12.2  案例：打印长方形
# 外层控制行数，内层控制每行打印几个字符。
# 注意变量含义：u 是每行的长度（列数），j 是行数。

# i = 0
# u = int(input("请输入长度"))     # 列数
# j = int(input("请输入宽度"))     # 行数
# while i < j:                    # 原写法是 while i <= j，会多打一行（off-by-one）
#     for k in range(u):
#         print("*", end="")
#     i += 1
#     print()

# --- 12.3  案例：打印九九乘法表
# 第 i 行只打印 i 个算式，所以内层是 range(1, i + 1) —— 这是「三角形」类图形的关键。

# for i in range(1, 10):              # 外层：1~9 行
#     for j in range(1, i + 1):       # 内层：第 i 行打 i 个
#         print(f"{j} x {i} = {j * i}", end="\t")
#     print()                         # 一行打完换行

# --- 12.4  案例：打印三角形
# 三角形的关键在「每行开头的空格数」和「每行的字符数」，用行号 k 推出来即可。
# 三种常见图形先看区别：
#     左对齐直角三角形        等腰三角形          数字三角形
#          *                     *                  1
#          **                   ***               1 2
#          ***                 *****            1 2 3

# 【图形一：左对齐直角三角形】你的原写法
# 原标题写的是「等腰三角形」，但这段实际打出来的是左对齐直角三角形 ——
# 每行只打了星号、没补前面的空格，所以左边是齐的。
# i = int(input("请输入三角形的边长"))
# for k in range(1, i + 1):
#     for _ in range(k):          # 第 k 行打 k 个
#         print("*", end="")
#     print()

# 【图形二：等腰三角形】在图形一的基础上补空格
# 第 k 行：先打 i - k 个空格，再打 2k - 1 个星号，左右对称就成等腰了。
# i = int(input("请输入等腰三角形的边长"))
# for k in range(1, i + 1):
#     for _ in range(i - k):
#         print(" ", end="")
#     for _ in range(2 * k - 1):
#         print("*", end="")
#     print()

# 上面内层那两个循环，用字符串重复（第 3.4 节）可以一行写完，效果完全一样：
# i = int(input("请输入等腰三角形的边长"))
# for k in range(1, i + 1):
#     print(" " * (i - k) + "*" * (2 * k - 1))

# 【图形三：数字直角三角形】你的原写法
# 原标题写的是「金字塔」（还有笔误「答应」），但这段打出来的是递增的数字直角三角形。
# i = int(input("请输入数值"))
# for j in range(1, i + 1):
#     for k in range(1, j + 1):
#         print(f"{k}", end="\t")
#     print()

# --- 12.5  案例：打印棋盘
# 8×8 的棋盘，相邻格子的字符要相反。

# 【写法一：打印完一行就对调两个字符】你的原写法（尺寸修正为 8×8）
# 原写法用的是 range(1, 10)，画出来是 9×9，不是标准棋盘。
# 思路：每个格子打「两」个字符，一行打完把 o、p 对调，下一行的底色就反过来了。
# o = "*"
# p = "0"
# for i in range(8):
#     for k in range(8):
#         print(f"{o}{p}", end="")
#     o, p = p, o         # 一行结束，对调 o 和 p
#     print()

# 【写法二：用行列下标的奇偶性判断】更简洁，推荐
# (i + k) 的奇偶性天然交替，不需要手动对调变量。
# for i in range(8):
#     for k in range(8):
#         if (i + k) % 2 == 0:
#             print("*", end="")
#         else:
#             print("0", end="")
#     print()


# =============================================================================
#  13  break 与 continue
# =============================================================================

# --- 13.1  break：立即结束整个循环
# 一遇到 break，循环马上跳出，剩下的次数一次都不再执行。
# 常用于「找到答案就收工」的场景。

# for i in range(1, 11):
#     if i == 5:
#         break               # 到 5 就跳出整个循环
#     print(i)                # 只打印 1 2 3 4

# --- 13.2  continue：跳过本次，继续下一轮
# continue 只结束「当前这一轮」，循环本身继续往下跑。

# for i in range(1, 11):
#     if i == 5:
#         continue            # 跳过 5 这一轮，继续跑 6
#     print(i)                # 打印 1 2 3 4 6 7 8 9 10，唯独没有 5

# --- 13.3  两者对比
#   break    = 整个循环不要了，直接结束
#   continue = 这一轮不要了，下一轮继续


# =============================================================================
#  14  random 模块与 while True
# =============================================================================

# --- 14.1  import：导入模块
# Python 自带很多现成的「工具箱」，叫模块；用 import 把工具箱拿进来才能用。
# random 是负责「随机」的工具箱。

# import random

# --- 14.2  random.randint(a, b)
# 随机生成一个 a 到 b 之间的整数，**含 a 和 b 两端**。
# 注意和 range() 的区别：range 不含结尾，randint 含两端。

# import random
# num = random.randint(1, 100)
# print(num)              # 每次运行结果都不一样

# --- 14.3  while True：无限循环 + break 出口
# while True 的条件永远成立，所以一定要在循环体里用 break 留一个出口，否则会死循环。
# 这种写法比「设一个 flag 变量」更直接，是交互式程序的常见套路（见 15.2 对比）。

# --- 14.4  案例：猜数字游戏
# 综合运用：random 造答案、while True 反复猜、if/elif 判断大小、break 结束。

# import random
# random_number = random.randint(1, 100)

# while True:                     # 原写法 while(True): 括号多余，Python 里可以省略
#     num = int(input("请输入一个数字"))
#     if num > random_number:
#         print("大了")
#     elif num < random_number:
#         print("小了")
#     else:
#         print("猜对了")
#         break                   # 猜中，跳出循环

# 可以自己加练：① 统计猜了几次；② 输入的不是数字时怎么办（会报 ValueError，见第 05 章）。


# =============================================================================
#  15  循环综合练习
# =============================================================================

# --- 15.1  登录验证（flag 变量法）—— 你的原写法
# 用一个 flag 变量 y 控制循环：y 为 1 时继续，登录成功就把 y 置 0 退出。
# 亮点：if not i or not o 用来拦截「空输入」——空字符串本身就是 False，not 一下即「为空」，
#       这是很规范的防呆写法。

# y = 1
# while y:                          # 原写法 while(y): 括号多余，可省略
#     i = input("请输入账号")         # input() 本来就返回字符串，不用再套 str()
#     o = input("请输入密码")
#     if not i or not o:
#         print("账号或密码不得为空")
#     elif (i == "c1" and o == "p1") or (i == "c2" and o == "p2") or (i == "c3" and o == "p3"):
#         print("登陆成功，欢迎使用")
#         y = 0                     # 成功，让循环条件变假，退出
#     else:
#         print("账号或密码错误，请重试")

# --- 15.2  同样的功能，用 while True 写（对照第 14 章）
# 不设 flag 变量，成功时直接 break，通常更直观。

# while True:
#     i = input("请输入账号")
#     o = input("请输入密码")
#     if not i or not o:
#         print("账号或密码不得为空")
#     elif (i == "c1" and o == "p1") or (i == "c2" and o == "p2") or (i == "c3" and o == "p3"):
#         print("登陆成功，欢迎使用")
#         break
#     else:
#         print("账号或密码错误，请重试")


# =============================================================================
#  16  字符串进阶（索引 · 切片 · 常用方法）
# =============================================================================
# 字符串的三个性质：有序、可迭代、**不可修改**。
# 记住「不可修改」，才能理解本章的铁律：
#   所有方法都不会改动原字符串，只会返回一个新字符串 —— 要用变量接住才生效。

# --- 16.1  索引（下标）
# 用方括号加编号取出「某一个」字符。
# 正向索引从 0 开始；反向索引从 -1 开始（-1 就是最后一个字符）。

# s = "hello-python"
# print(len(s))     # 12      求长度
# print(s[0])       # h       第 1 个字符
# print(s[4])       # o       第 5 个字符
# print(s[-1])      # n       最后一个字符
# print(s[-3])      # h       倒数第 3 个字符
# #print(s[100])    # 索引越界会报 IndexError: string index out of range（取消注释可复现）

# --- 16.2  切片  s[开始索引 : 结束索引 : 步长]
# 左闭右开：包含「开始」，不包含「结束」—— 和 range() 一个规矩。
#   省略开始 = 从 0 开始     省略结束 = 取到末尾     省略步长 = 默认 1
#   步长为正 = 从左往右取    步长为负 = 从右往左取

# s = "hello-python"
# print(s[0:5:1])     # hello
# print(s[:5:1])      # hello             省略开始
# print(s[:5:])       # hello             省略步长
# print(s[:5])        # hello             全都省略，最简写法
# print(s[6:])        # python            取到末尾
# print(s[::-1])      # nohtyp-olleh      步长为负，字符串反转
# print(s[-1:5:-1])   # nohtyp            从倒数第 1 个往前取到下标 5（不含 5）
# print(s[5:100])     # -python           切片越界不报错，自动截断到边界
# print(s[4:1])       # （空字符串）       开始 > 结束，方向反了

# --- 16.3  常用方法
# 调用格式：字符串.方法名()，中间的点读作「的」；方法名后面必须有括号。
# 本节示例统一用 s = "Hello Python"。
#   find()        在字符串中查找子串，返回第一次出现的索引位置，找不到返回 -1
#   count()       统计子串在字符串中出现的次数
#   upper()       把所有字母转成大写
#   lower()       把所有字母转成小写
#   split()       按指定分隔符分割成列表
#   strip()       去掉两端的空白字符或指定字符
#   replace()     把指定子串替换成新的子串
#   startswith()  检查是否以指定子串开头，返回布尔值

# s = "Hello Python"
# print(s.find('Python'))         # 6        找到了，返回第一次出现的索引
# print(s.find('Java'))           # -1       找不到返回 -1，常用来判断「有没有」
# print(s.count('H'))             # 1
# print(s.count('o'))             # 2
# print(s.upper())                # HELLO PYTHON
# print(s.lower())                # hello python
# print(s.split(' '))             # ['Hello', 'Python']   注意结果是「列表」
# print(s.split('o'))             # ['Hell', ' Pyth', 'n']  分隔符本身会被吃掉
# print(s.strip())                # Hello Python   两端没有空白，原样返回
# print('  Hello  '.strip())      # Hello
# print('**Hello**'.strip('*'))   # Hello          只去两端，中间的动不了
# print(s.replace('H', 'C'))      # Cello Python
# print(s.startswith('H'))        # True

# --- 16.4  几个同样常用的补充方法
#   endswith()         是否以指定子串结尾
#   index()            同 find()，但找不到会直接报 ValueError
#   lstrip() / rstrip()  只去左端 / 只去右端
#   join()             用字符串把列表元素连起来（split 的反操作）
#   isdigit()          是否全是数字，常用来校验输入

# print(s.endswith('n'))                  # True
# print('**Hello**'.lstrip('*'))          # Hello**
# print('**Hello**'.rstrip('*'))          # **Hello
# print('-'.join(['a', 'b', 'c']))        # a-b-c
# print('123'.isdigit())                  # True
# print('12a'.isdigit())                  # False

# isdigit() 配合 input() 挡住非数字输入（回顾第 05 章的 ValueError）
# num = input("请输入一个数字：")
# if num.isdigit():
#     print(int(num) * 2)
# else:
#     print("输入的不是数字")

# --- 16.5  关键：方法不会改原字符串
# 字符串是不可修改的，方法只负责「算出并返回一个新字符串」，原变量一个字符都不变。

# s = "Hello Python"
# s.upper()               # 返回值被丢掉了，s 一点没变
# print(s)                # Hello Python
# s = s.upper()           # 正确写法：用变量接住
# print(s)                # HELLO PYTHON

# --- 16.6  易错点
#   s.upper() 单独写一行        -> 没接住返回值，等于白写
#   s.upper（漏括号）           -> 拿到的是方法本身，不是结果
#   find() 与 index() 混用      -> find 找不到返回 -1，index 找不到直接报错
#   以为 split() 返回字符串     -> 返回的是列表，后面要按列表处理
#   strip('*') 以为能去中间的 * -> 只去两端
#   切片与索引搞混              -> 切片越界不报错，索引越界报 IndexError


# =============================================================================
#  17  数据容器：列表 list
# =============================================================================
# 前面几章处理的都是「一个值」，列表用来装「一堆值」。
# 列表四特点：可存不同类型 · 有序 · 可重复 · 可修改。
# 和字符串最关键的区别：字符串不可修改，列表可以修改（见 16.5）。

# --- 17.1  定义
# 用方括号 [] 定义，元素之间用逗号隔开。
# 注意：千万别用 list 当变量名 —— list 是 Python 内置的类型名，会被遮住。

# nums = [80, 60, 80, 90, 56, '元素1', "元素2"]
# print(nums)          # [80, 60, 80, 90, 56, '元素1', '元素2']
# print(len(nums))     # 7

# --- 17.2  索引与切片（规则和第 16 章完全一样）
# 正向从 0 开始，反向从 -1 开始，切片左闭右开。

# nums = [80, 60, 80, 90, 56, '元素1', "元素2"]
# print(nums[0])       # 80              第 1 个元素
# print(nums[-7])      # 80              倒数第 7 个（长度是 7，就是第 1 个）
# print(nums[-1])      # 元素2            最后一个元素
# print(nums[0:5:2])   # [80, 80, 56]    从 0 取到 4，步长 2
# #print(nums[100])    # IndexError: list index out of range（列表和字符串的报错文字不一样）

# --- 17.3  修改与删除
# 改：直接给下标赋新值（这是字符串做不到的事）
# 删：del 是语句不是方法，按索引删

# nums = [80, 60, 80, 90, 56, '元素1', "元素2"]
# nums[0] = 90         # 把第 1 个元素改成 90
# del nums[5]          # 删掉下标 5 的元素
# print(nums)          # [90, 60, 80, 90, 56, '元素2']

# --- 17.4  常用方法
#   append()    在列表尾部追加一个元素                    nums.append("新元素")
#   insert()    在指定索引之前插入元素，后面的元素整体后移   nums.insert(0, "新元素")
#   remove()    按「值」删除，删掉从头开始第一个匹配到的     nums.remove("元素1")
#   pop()       按「索引」删除，不写参数删最后一个，会返回被删的值
#   sort()      排序，只能同类型数据才能排                 nums.sort()
#   reverse()   反转顺序                                 nums.reverse()

# 下面每个方法都从一个全新的 nums 开始，方便单独对照。

# nums = [80, 60, 80, 90, 56, '元素1', "元素2"]
# nums.append("增加的元素")
# print(nums)          # [80, 60, 80, 90, 56, '元素1', '元素2', '增加的元素']   末尾追加

# nums = [80, 60, 80, 90, 56, '元素1', "元素2"]
# nums.insert(5, "插入的元素")
# print(nums)          # [80, 60, 80, 90, 56, '插入的元素', '元素1', '元素2']   插到下标 5，后面整体后移

# nums = [80, 60, 80, 90, 56, '元素1', "元素2"]
# nums.remove("元素1")
# print(nums)          # [80, 60, 80, 90, 56, '元素2']                          按值删

# nums = [80, 60, 80, 90, 56, '元素1', "元素2"]
# num = nums.pop(2)
# print(num)           # 80                                                     pop 会把删掉的值返回
# print(nums)          # [80, 60, 90, 56, '元素1', '元素2']                     按索引删

# 排序与反转
# nums = [90, 56, 80, 60]
# nums.sort()
# print(nums)          # [56, 60, 80, 90]      从小到大
# nums.reverse()
# print(nums)          # [90, 80, 60, 56]      顺序反转

# --- 17.5  三种删除方式的区别
#   nums.remove(值)     按值删     没有返回值
#   nums.pop(索引)      按索引删   有返回值，返回被删掉的那个值
#   del nums[索引]      按索引删   没有返回值（del 是语句，不是方法）

# --- 17.6  列表推导式
# 作用：一行按规则生成一个新列表，替代「建空列表 + for 循环 append」。
#   格式一：[要插入的值 for i in 序列]
#   格式二：[要插入的值 for i in 序列 if 条件]
# 「要插入的值」可以是表达式，比如 i ** 2。

# 写法一：for + append
# nums = []
# for i in range(1, 21):
#     nums.append(i ** 2)
# print(nums)
# 写法二：列表推导式 —— 一行搞定，结果完全一样
# nums = [i ** 2 for i in range(1, 21)]
# print(nums)                              # [1, 4, 9, 16, ... 400]

# 带条件：只取偶数再平方
# print([i ** 2 for i in range(1, 21) if i % 2 == 0])
#                       # [4, 16, 36, 64, 100, 144, 196, 256, 324, 400]

# --- 17.7  案例与练习
# ① 生成 1~20 的平方
# print([i ** 2 for i in range(1, 21)])

# ② 把 1~30 里能被 3 或 5 整除的数提出来，并平方
# list1 = list(range(1, 31))               # 等价于手写 1~30 那一长串数字
# print([i ** 2 for i in list1 if i % 3 == 0 or i % 5 == 0])
#                       # [9, 25, 36, 81, 100, 144, 225, 324, 400, 441, 576, 625, 729, 900]

# ③ 把列表里的正数提出来，再从小到大排序
# list2 = [11, 2, 31, 4, -5, 15, 17, 28, 49, 10, -11, 16, 54, -14, 36, -16, 87, -39]
# new_list = [i for i in list2 if i > 0]
# new_list.sort()
# print(new_list)      # [2, 4, 10, 11, 15, 16, 17, 28, 31, 36, 49, 54, 87]

# --- 17.8  易错点
#   用 list 当变量名        -> 会遮住内置的 list 类型，改用 nums / scores
#   nums = nums.sort()      -> sort() 返回 None，这么写列表就变成 None 了
#   remove 与 pop 混用      -> 按值删用 remove，按索引删用 pop
#   remove 一个不存在的值   -> ValueError: list.remove(x): x not in list
#   混类型列表调用 sort()   -> TypeError，sort() 只能排同类型数据
#   以为切片会改原列表      -> 切片返回新列表，原列表不受影响


# =============================================================================
#  18  字符串综合案例
# =============================================================================

# --- 18.1  邮箱格式验证
# 需求：输入邮箱，验证格式（恰好一个 @、至少一个 .），
#       正确输出「邮箱格式正确」，否则输出「邮箱格式错误」。
# 用的正是 16.3 的 count()：数一数某个字符出现了几次。
#   @ 要「恰好 1 个」 -> 用 == 1        . 要「至少 1 个」 -> 用 >= 1

# str_mail = input("请输入您的邮箱")
# if str_mail.count("@") == 1 and str_mail.count(".") >= 1:
#     print("邮箱格式正确")
# else:
#     print("邮箱格式错误")

# 写法二：用 in 运算符 —— 判断「子串在不在字符串里」，返回布尔值，更直观。
# 区别：in 只能判断「有没有」，管不住「有两个 @」的情况。

# str_mail = input("请输入您的邮箱")
# if "@" in str_mail and "." in str_mail:
#     print("邮箱格式正确")
# else:
#     print("邮箱格式错误")

# --- 18.2  判断回文
# 回文：正着读和倒着读一样的字符串，比如「上海自来水来自海上」。
# 思路：把字符串反转一份，跟原串比较，相等就是回文。反转用 16.2 的切片 [::-1]。

# str_wen = input("请输入一个字符串")
# str_wen2 = ""
# for i in str_wen[::-1]:      # 先反转，再一个字符一个字符取出来
#     str_wen2 += i            # += 就是 str_wen2 = str_wen2 + i
# if str_wen == str_wen2:
#     print("你输入的是回文")
# else:
#     print("你输入的不是回文")

# 更简单的一行写法：上面那个循环做的其实就是「反转」，而 [::-1] 本来就能直接反转
# str_wen = input("请输入一个字符串")
# if str_wen == str_wen[::-1]:
#     print("你输入的是回文")
# else:
#     print("你输入的不是回文")

# --- 18.3  字符串反转 + 转大写 + 存进列表
# 把第 17 章的列表、第 16 章的切片反转和 upper() 串起来用。

# str_list = input("请输入字符串")
# new_list = []
# for i in str_list[::-1]:
#     new_list.append(i.upper())
# for k in new_list:
#     print(k)
# 输入 abc -> 依次输出 C B A，new_list 是 ['C', 'B', 'A']
# 也可以用列表推导式一行写完（见 17.6）：
# new_list = [i.upper() for i in str_list[::-1]]


# =============================================================================
#  待整理
# =============================================================================
# 元组 tuple —— 课堂还没讲完，下次补
