# Python 学习笔记

> 配套：`python.py`（速查手册 · 示例代码）  ·  本文件（讲原理 · 表格速查）
> 两文件章节编号一一对应，可互相对照。

## 目录

| 章 | 主题 |
| --- | --- |
| 01 | 变量与基本输出 |
| 02 | 数据类型与查看 `type()` |
| 03 | 字符串基础（定义 · 转义 · 拼接 · 重复） |
| 04 | 字符串格式化（`%` 占位符 · f-string） |
| 05 | 输入 `input()` |
| 06 | 运算符（算术 · 赋值 · 比较 · 逻辑） |
| 07 | 条件判断 `if` / `if...else` |
| 08 | 分支结构 `if...elif...else` / `match...case` |
| 09 | 综合练习 |
| 10 | 循环 `while` |
| 11 | 循环 `for` 与 `range()` |
| 12 | 嵌套循环 |
| 13 | `break` 与 `continue` |
| 14 | `random` 模块与 `while True` |
| 15 | 循环综合练习 |
| 16 | 字符串进阶（索引 · 切片 · 常用方法） |
| 17 | 数据容器：列表 `list` |
| 18 | 字符串综合案例 |

---

## 01 变量与基本输出

### 1.1 `print()`

- 把内容打印到控制台，字符串要用引号括起来。
- 一次可以打印多个值，用逗号隔开，输出时自动以空格分隔。

### 1.2 变量

- 变量是给数据起的名字，用 `=` 赋值：`num = 10`。
- **赋值方向**：先算等号右边，再把结果存进左边的变量。
- 变量可以**反复赋值**，新值会覆盖旧值：

  ```python
  num = 10
  num = num + 5    # 右边先算得 15，再存回 num
  ```

- 变量名要有意义，用下划线连接多个单词：`user_name`、`total_price`。

### 1.3 `print()` 的 `end` 参数

| 写法 | 效果 |
| --- | --- |
| `print("a")` | 输出 `a` 后**自动换行** |
| `print("a", end="")` | 输出 `a` 后**不换行**，下一次 print 接在后面 |
| `print("a", end="\t")` | 结尾加一个制表符，做表格对齐 |

- 画图形时几乎必用（见第 12 章）。

  ```python
  print("a", end="")
  print("b", end="")
  print("c")          # 合起来输出：abc
  ```

---

## 02 数据类型与查看 `type()`

### 2.1 字面量类型及书写格式

#### 数字类型

| 类型 | 说明 | 示例 |
| --- | --- | --- |
| 整数 (int) | 不带小数点的数 | `10`  `18`  `-5`  `0` |
| 浮点数 / 小数 (float) | 带小数点的数 | `8.5`  `3.14`  `1.0`  `-3.5` |

#### 布尔 (bool)

- 表达现实生活中的逻辑：真或假 → `True`(1) / `False`(0)
- 布尔本质就是数字：参与数学运算时，`True` 转成 1，`False` 转成 0。

#### 字符串 (str)

- 描述文本的数据类型，例如：`"人生苦短，我用 Python"`

#### 空值 (NoneType)

- 表示空或无值，仅包含一个值：`None`

#### 数据容器

- 存储多项数据的容器：列表 / 元组 / 集合 / 字典

### 2.2 类型查看与判断

- `type()`：查看数据类型
- `isinstance(num, int)`：判断 num 是否为 int 类型，输出 `True` / `False`

| 写法 | 结果 |
| --- | --- |
| `type(10)` | `<class 'int'>` |
| `type(3.14)` | `<class 'float'>` |
| `type(True)` | `<class 'bool'>` |
| `type("abc")` | `<class 'str'>` |
| `type(None)` | `<class 'NoneType'>` |

---

## 03 字符串基础（定义 · 转义 · 拼接 · 重复）

### 3.1 字符串的定义方式

- 单行字符串：`s = "单行字符串"` 或 `s = '单行字符串'`（单双引号等价）
- 多行字符串：使用三个双引号，换行和缩进都会原样保留。

  ```python
  s = """多行
  字符串"""
  ```

### 3.2 常见转义字符

| 转义符 | 含义 |
| --- | --- |
| `\'` | 单引号 |
| `\"` | 双引号 |
| `\n` | 换行 |
| `\t` | 制表符 |
| `\\` | 反斜杠本身 |

> 转义符 `\` 的作用：让后面那个字符失去原本的特殊含义，变成一个普通字符。

### 3.3 字符串拼接（`+` 号）

- `+` 号**只能拼接字符串**，两个字符串直接相连。
- 想拼接数字，必须先转换类型，否则报错：

  ```python
  name = "涛哥"
  age = 18
  print("我叫" + name + "今年" + str(age) + "岁")   # str() 转成字符串
  ```

> 拼接多个值时容易漏掉空格和标点，实际开发更推荐用 **`04` 的格式化**写法。

### 3.4 字符串重复（`*` 号）

- 字符串乘以一个整数，等于把它重复几遍。

| 写法 | 结果 |
| --- | --- |
| `"*" * 5` | `*****` |
| `"-" * 10` | `----------` |
| `"ab" * 3` | `ababab` |

> 配合第 12 章画图形，可以省掉一整层内循环。

---

## 04 字符串格式化（`%` 占位符 · f-string）

### 4.1 `%` 占位符

- 在字符串里挖一个坑，后面用 `%` 把值填进去。

| 占位符 | 对应类型 |
| --- | --- |
| `%s` | 字符串（也可接数字，会自动转换） |
| `%d` | 整数 |
| `%f` | 小数 |

- **单变量**：`print("我叫%s" % S1)`
- **多变量**：值用括号包起来，按顺序一一对应。

  ```python
  print("我叫%s,今年%s" % (S1, S2))
  ```

### 4.2 f-string（推荐写法）

- 在字符串前加 `f`，用 `{}` 直接嵌入变量或表达式。

  ```python
  name = "小牛子"
  age = 18
  print(f"我叫{name},今年{age}岁")
  ```

- `{}` 里可以写**任意表达式**，直接参与运算：

  ```python
  print(f"两数之和是{num1 + num2}")
  ```

| 写法 | 示例 | 评价 |
| --- | --- | --- |
| `+` 拼接 | `"我叫" + name` | 要手动 `str()`，易出错 |
| `%` 占位符 | `"我叫%s" % name` | 老式写法，仍常见 |
| f-string | `f"我叫{name}"` | **推荐**，最直观 |

---

## 05 输入 `input()`

- `input()` 会让程序暂停，等用户输入后回车，再继续往下执行。
- **返回值永远是字符串类型** —— 这是最关键的一点。

  ```python
  name = input("请输入你的姓名：")
  print(f"你好！{name}")
  ```

- 想做数学运算，必须先用 `int()` / `float()` 转换成数字：

| 转换函数 | 作用 | 示例 |
| --- | --- | --- |
| `int("18")` | 转成整数 | `18` |
| `float("3.14")` | 转成小数 | `3.14` |
| `str(18)` | 转成字符串 | `"18"` |

  ```python
  num = int(input("请输入一个数字："))    # 先转类型，再参与运算
  print(f"这个数字的两倍是{num * 2}")
  ```

> 常见报错 `ValueError`：输入的字符串无法转成数字（比如输入了「abc」却用 `int()`）。

---

## 06 运算符（算术 · 赋值 · 比较 · 逻辑）

### 6.1 算术运算符

| 运算符 | 名称 | 示例 | 说明 |
| --- | --- | --- | --- |
| `+` | 加法 | `1 + 5 = 6` | |
| `-` | 减法 | `8 - 3 = 5` | |
| `*` | 乘法 | `3 * 8 = 24` | |
| `/` | 除法 | `10 / 5 = 2.0` | 除法结果为小数 |
| `//` | 整除 | `10 // 3 = 3` | 整除结果为整数 |
| `%` | 取余 | `10 % 3 = 1` | 10 除以 3 的余数，也叫求模 |
| `**` | 幂指数 | `10 ** 3 = 1000` | 10 的三次方 |

### 6.2 赋值运算符

| 运算符 | 名称 | 示例 | 说明 |
| --- | --- | --- | --- |
| `=` | 赋值 | `a = 10` | 把 10 赋给变量 a |
| `+=` | 加法赋值 | `a += 3` | 等价于 `a = a + 3` |
| `-=` | 减法赋值 | `a -= 3` | 等价于 `a = a - 3` |
| `*=` | 乘法赋值 | `a *= 3` | 等价于 `a = a * 3` |
| `/=` | 除法赋值 | `a /= 3` | 等价于 `a = a / 3` |
| `//=` | 整除赋值 | `a //= 3` | 等价于 `a = a // 3` |
| `%=` | 取余赋值 | `a %= 3` | 等价于 `a = a % 3` |
| `**=` | 幂赋值 | `a **= 3` | 等价于 `a = a ** 3` |

> `+=` 在计数、累加场景里用得最多，循环里几乎必用。

### 6.3 比较运算符

| 运算符 | 名称 | 示例 | 结果 |
| --- | --- | --- | --- |
| `==` | 等于 | `3 == 3` | `True` |
| `!=` | 不等于 | `3 != 5` | `True` |
| `>` | 大于 | `5 > 3` | `True` |
| `<` | 小于 | `3 < 5` | `True` |
| `>=` | 大于等于 | `5 >= 5` | `True` |
| `<=` | 小于等于 | `3 <= 5` | `True` |

> 比较运算的结果是布尔值 `True` / `False`。
> 注意区分：`=` 是赋值，`==` 才是判断相等。

### 6.4 逻辑运算符

| 运算符 | 名称 | 示例 | 结果 | 说明 |
| --- | --- | --- | --- | --- |
| `and` | 与 | `True and False` | `False` | 两边都为真才为真 |
| `or` | 或 | `True or False` | `True` | 有一边为真就为真 |
| `not` | 非 | `not True` | `False` | 取反 |

- 用括号组合条件，可读性更好：

  ```python
  if (num % 4 == 0 and num % 100 != 0) or num % 400 == 0:
      print("是闰年")
  ```

---

## 07 条件判断 `if` / `if...else`

### 7.1 语法

```python
if 条件:
    条件成立时执行的代码
```

- **冒号 `:`** 和 **缩进**是 Python 的硬性要求，缺一不可。
- 缩进一般为 4 个空格；同一代码块必须缩进相同。
- 条件为 `True` 才执行缩进的代码块，否则整块跳过。

### 7.2 `if...else`

```python
if 条件:
    条件成立时执行的代码
else:
    条件不成立时执行的代码
```

| 写法 | 特点 |
| --- | --- |
| 写两个 `if` | 两个条件都会被检查，可能都执行 |
| `if...else` | 二选一，只会执行其中一个，更清晰 |

> 判断互斥的两种情况时，一律用 `if...else`，不要写两个 `if`。

---

## 08 分支结构 `if...elif...else` / `match...case`

### 8.1 `if...elif...else`

```python
if 条件1:
    执行的代码1
elif 条件2:
    执行的代码2
else:
    条件都不成立时执行的代码3
```

- **从上往下依次判断，命中一个就跳出**，后面的条件不再检查。
- `elif` 可以有多个，`else` 最多一个（也可省略）。
- 适合条件有**先后顺序**的场景（如分数分段、等级划分）。

### 8.2 `match...case`

```python
match 要判断的条件:
    case 值1:
        执行的代码1
    case 值2:
        执行的代码2
    case _:
        条件都不匹配时执行的代码3
```

- 需要 **Python 3.10+**（本机 3.14 支持）。
- 判断的是**具体值相等**，比一长串 `elif` 更直观。
- `case _` 相当于 `else`，兜底所有未匹配的情况。

| 结构 | 适用场景 |
| --- | --- |
| `if...elif` | 条件是比较、范围（`>`、`<`、`and`） |
| `match...case` | 条件是若干**固定值**的匹配（如运算符、菜单选项） |

---

## 09 综合练习

> 本节的完整代码见 `python.py` 第 09 章，本章记录解题思路。

- 会员准入：`vip >= 1 and token <= 2000` —— 两个条件同时成立才为真。
- 取款计算：剩余金额 = 余额 − 取出金额，注意先用 `float()` 转换输入。

---

## 10 循环 `while`

### 10.1 基本语法

```python
while 条件:
    重复执行的代码
    更新变量
```

- 只要条件为 `True`，就重复执行缩进的代码块；条件变 `False` 时结束。

### 10.2 循环三要素（缺一不可）

| 要素 | 位置 | 作用 |
| --- | --- | --- |
| ① 初始化变量 | 循环之前 | 给循环一个起点 |
| ② 判断条件 | `while` 之后 | 决定什么时候停 |
| ③ 更新变量 | 循环体内 | 让条件终有一天变 `False` |

> **死循环**：漏掉第 ③ 步（忘记更新变量），条件永远为真，程序会一直跑下去。

### 10.3 典型模式：累加

```python
total = 0                 # ① 累加器，初始为 0
num = 0
while num <= 100:         # ② 判断
    if num % 2 == 0:
        total += num      # 累加
    num += 1              # ③ 更新
print(total)
```

---

## 11 循环 `for` 与 `range()`

### 11.1 基本语法

```python
for 变量 in 序列:
    重复执行的代码
```

- `for` 会把序列里的元素**一个一个取出来**，每取一个就执行一遍循环体。
- 遍历次数由序列长度决定，**不会死循环**，比 `while` 更省心。

### 11.2 遍历字符串

```python
for i in msg:
    print(i)          # 一个字符一个字符地打印
print("遍历结束")      # 没有缩进，循环全部跑完才执行
```

> 缩进决定语句属于循环内还是循环外，这是最容易搞混的地方。

### 11.3 `range()` 的三种用法

`range()` 生成一段整数序列，专门配合 `for` 使用。
**共同点：生成的数字都不含结尾那个数（左闭右开）。**

| 写法 | 含义 | 示例 | 生成 |
| --- | --- | --- | --- |
| `range(end)` | 从 0 到 end-1 | `range(5)` | `0 1 2 3 4` |
| `range(start, end)` | 从 start 到 end-1 | `range(2, 8)` | `2 3 4 5 6 7` |
| `range(start, end, step)` | 每次跳 step 步 | `range(1, 10, 2)` | `1 3 5 7 9` |

- 想数 0~100，要写 `range(101)`，**不是** `range(100)`。

### 11.4 `while` 与 `for` 怎么选

| | `while` | `for` |
| --- | --- | --- |
| 循环次数 | 事先不确定 | 事先确定（遍历序列） |
| 风险 | 忘记更新变量会死循环 | 不会死循环 |
| 典型场景 | 等条件满足、用户交互 | 遍历字符串 / 列表 / `range()` |

> 能确定次数的循环，优先用 `for`。

---

## 12 嵌套循环

### 12.1 概念与执行顺序

- 一个循环里再套一个循环，叫**嵌套循环**。
- **执行顺序**：外层转一次，内层就完整转一圈。
- **总执行次数 = 外层次数 × 内层次数**。
- 典型用途：打印有行有列的图形 —— **外层控制「行」，内层控制「每行里的每一列」**。

```python
for i in range(3):          # 外层：3 行
    for k in range(4):      # 内层：每行 4 个
        print("*", end="")
    print()                 # 换行写在外层（没缩进），否则每打一个就换行
```

> **最常见的错误**：把 `print()` 换行写进了内层，结果每个字符都占一行。

### 12.2 常见图形套路

图形类题目的核心只有两个值：**每行开头的空格数**、**每行打印的字符数**。
用行号 `k` 把这两个值推出来，图形就成型了。

| 图形 | 空格数（第 k 行） | 字符数（第 k 行） |
| --- | --- | --- |
| 左对齐直角三角形 | `0` | `k` |
| 等腰三角形 | `i - k` | `2k - 1` |
| 数字直角三角形 | `0` | `k`（打印数字 1~k） |

```python
# 等腰三角形：空格递减 + 星号递增，两边对称
i = 4
for k in range(1, i + 1):
    print(" " * (i - k) + "*" * (2 * k - 1))
```

### 12.3 棋盘类图形

8×8 棋盘要求**相邻格子的字符相反**，两种常见写法：

| 写法 | 思路 | 特点 |
| --- | --- | --- |
| 对调变量 | 一行打完，把两个字符对调 | 需要额外两个变量 |
| 奇偶性判断 | `(行 + 列) % 2` 天然交替 | **推荐**，更简洁 |

```python
for i in range(8):
    for k in range(8):
        if (i + k) % 2 == 0:
            print("*", end="")
        else:
            print("0", end="")
    print()
```

---

## 13 `break` 与 `continue`

| 关键字 | 作用 | 记忆 |
| --- | --- | --- |
| `break` | **立即结束整个循环** | 整个循环不要了 |
| `continue` | **跳过本轮，继续下一轮** | 这一轮不要了 |

```python
for i in range(1, 11):
    if i == 5:
        break
    print(i)        # 输出 1 2 3 4

for i in range(1, 11):
    if i == 5:
        continue
    print(i)        # 输出 1 2 3 4 6 7 8 9 10（唯独没有 5）
```

> 两者通常都配合 `if` 使用，用来在循环中途改变流程。

---

## 14 `random` 模块与 `while True`

### 14.1 `import`：导入模块

- Python 自带很多现成的「工具箱」，称为**模块**。
- 用 `import 模块名` 把工具箱拿进来，才能用里面的功能。

```python
import random
```

### 14.2 `random.randint(a, b)`

- 随机生成一个 `a` 到 `b` 之间的整数。
- **含两端**（与 `range()` 不同，`range` 不含结尾）。

| 写法 | 结果 |
| --- | --- |
| `random.randint(1, 100)` | 1~100 之间的随机整数，每次都不同 |

### 14.3 `while True` 模式

```python
while True:
    ...              # 循环体
    if 退出条件:
        break        # 唯一的出口
```

- `while True` 条件永远成立，**必须在循环体里用 `break` 留出口**，否则死循环。
- 这是交互式程序（反复让用户输入直到满足条件）的常见套路。

| 控制循环的方式 | 写法 | 特点 |
| --- | --- | --- |
| flag 变量 | `y = 1` … `while y:` … `y = 0` | 逻辑显式，变量多 |
| `while True` + `break` | `while True:` … `break` | **更直接**，推荐 |

---

## 15 循环综合练习

### 15.1 判断空输入

- 空字符串在 Python 里就是 `False`，所以 `not i` 即「i 为空」。

```python
if not i or not o:
    print("账号或密码不得为空")
```

> 这是**很规范的防呆写法**，用户直接回车时不会误判。

### 15.2 循环重试（登录验证）

- 需求：输错能一直重试，成功才退出 —— 用循环包住输入。
- 两种写法（见 14.3 的对比）：

  ```python
  # 写法一：flag 变量
  y = 1
  while y:
      i = input("请输入账号")
      o = input("请输入密码")
      if not i or not o:
          print("账号或密码不得为空")
      elif (i == "c1" and o == "p1") or (i == "c2" and o == "p2"):
          print("登陆成功，欢迎使用")
          y = 0
      else:
          print("账号或密码错误，请重试")
  ```

  ```python
  # 写法二：while True + break（更推荐）
  while True:
      i = input("请输入账号")
      o = input("请输入密码")
      if not i or not o:
          print("账号或密码不得为空")
      elif (i == "c1" and o == "p1") or (i == "c2" and o == "p2"):
          print("登陆成功，欢迎使用")
          break
      else:
          print("账号或密码错误，请重试")
  ```

### 15.3 易错点小结

| 易错点 | 正确做法 |
| --- | --- |
| `while(y):` 加括号 | Python 里写 `while y:`，括号多余 |
| `str(input())` 套一层转换 | `input()` 本来就返回字符串，不用再 `str()` |
| `range(1, 10)` 以为是 1~10 | 是 1~9，**不含结尾** |
| 循环条件差一位 | 画 3 行就用 `while i < 3` 或 `range(3)`，不是 `<=` |
| `int(input())` 直接接输入 | 用户输入非数字会报 `ValueError`，必要时需校验 |

---

## 16 字符串进阶（索引 · 切片 · 常用方法）

> 字符串的三个性质：**有序**（每个字符都有编号）、**可迭代**（能用 `for` 逐个取出）、**不可修改**（一旦定义，里面的字符就改不了）。
> 记住「不可修改」，才能理解本章所有方法的一条铁律：**方法不会改动原字符串，只会返回一个新字符串**。

### 16.1 索引（下标）

用方括号加编号，取出**某一个**字符。

```python
s = "hello-python"
```

| 写法 | 含义 | 结果 |
| --- | --- | --- |
| `s[0]` | 第 1 个字符（正向索引从 0 开始） | `'h'` |
| `s[4]` | 第 5 个字符 | `'o'` |
| `s[-1]` | 最后一个字符（反向索引从 -1 开始） | `'n'` |
| `s[-3]` | 倒数第 3 个字符 | `'h'` |
| `len(s)` | 求长度，一共 12 个字符 | `12` |

- **正向索引从 0 开始**，最后一个字符的下标是 `11`（= 长度 − 1）。
- **反向索引从 -1 开始**，`-1` 就是最后一个字符。
- 下标超出范围会报错：`IndexError: string index out of range`。

### 16.2 切片

用冒号一次取出**一段**字符。完整语法：

```
s[开始索引 : 结束索引 : 步长]
```

**左闭右开**：包含「开始」那个字符，**不包含**「结束」那个字符 —— 和 `range()` 的规则完全一样。

| 写法 | 含义 | 结果 |
| --- | --- | --- |
| `s[0:5:1]` | 从 0 取到 4，每次走 1 步 | `'hello'` |
| `s[:5:1]` | 省略开始 = 从 0 开始 | `'hello'` |
| `s[:5:]` | 省略步长 = 每次走 1 步 | `'hello'` |
| `s[:5]` | 全都省略 = 最简写法 | `'hello'` |
| `s[6:]` | 从 6 一直取到最后一个字符 | `'python'` |
| `s[::-1]` | 步长为负 = 从后往前取（**反转字符串**） | `'nohtyp-olleh'` |
| `s[-1:5:-1]` | 从倒数第 1 个往前取，取到下标 5 为止（不含 5） | `'nohtyp'` |
| `s[5:100]` | 结束索引超出范围 | `'-python'`（自动截断，**不报错**） |
| `s[4:1]` | 开始 > 结束，方向反了 | `''`（空字符串） |

- **步长为正**：从左往右截取；**步长为负**：从右往左截取。
- 三条省略规则：省略开始 = 从 0 开始；省略结束 = 取到末尾；省略步长 = 默认为 1。
- **切片越界不报错**，只会截断到边界；而索引越界会报错 —— 这是两者最大的区别。

### 16.3 常用方法

方法就是「字符串自带的功能」，调用格式是 `字符串.方法名()`，中间那个点读作「的」：`s.upper()` 就是「s 的 upper」。

本节示例统一用 `s = "Hello Python"`。

| 方法 | 作用 | 示例 | 结果 |
| --- | --- | --- | --- |
| `find()` | 在字符串中查找子串，返回第一次出现的索引位置，找不到返回 -1 | `s.find('Python')` | `6` |
| `count()` | 统计子串在字符串中出现的次数 | `s.count('H')` | `1` |
| `upper()` | 将字符串中的所有字母转换为大写 | `s.upper()` | `'HELLO PYTHON'` |
| `lower()` | 将字符串中的所有字母转换为小写 | `s.lower()` | `'hello python'` |
| `split()` | 将字符串按指定分隔符分割成列表 | `s.split(' ')` | `['Hello', 'Python']` |
| `strip()` | 去除字符串两端的空白字符或指定字符 | `s.strip()` | `'Hello Python'` |
| `replace()` | 将字符串中的指定子串替换为新的子串 | `s.replace('H', 'C')` | `'Cello Python'` |
| `startswith()` | 检查字符串是否以指定子串开头，返回布尔值 | `s.startswith('H')` | `True` |

- `find()` 找不到时的返回值 **-1** 很关键，常用来判断「有没有」：

  ```python
  print(s.find('Java'))     # -1，说明没找到
  ```

- `split()` 的返回值是**列表**，不是字符串；不写参数时按空白字符切分。

  ```python
  print(s.split(' '))       # ['Hello', 'Python']
  print(s.split('o'))       # ['Hell', ' Pyth', 'n']（分隔符会被吃掉）
  ```

- `strip()` **只去两端**，中间的字符动不了；不带参数时去的是空白（空格、制表符、换行）。

  ```python
  print('  Hello  '.strip())      # 'Hello'
  print('**Hello**'.strip('*'))   # 'Hello'
  ```

### 16.4 几个同样常用的补充方法

| 方法 | 作用 | 示例 | 结果 |
| --- | --- | --- | --- |
| `endswith()` | 检查字符串是否以指定子串结尾 | `s.endswith('n')` | `True` |
| `index()` | 同 `find()`，但找不到会**报错** | `s.index('Java')` | `ValueError` |
| `lstrip()` / `rstrip()` | 只去左端 / 只去右端 | `'**Hello**'.lstrip('*')` | `'Hello**'` |
| `join()` | 用字符串把列表里的元素连起来（`split()` 的反操作） | `'-'.join(['a','b','c'])` | `'a-b-c'` |
| `isdigit()` | 判断是否全是数字，常用来校验输入 | `'123'.isdigit()` | `True` |

- `find()` 与 `index()` 只差一点：**找不到时**，`find()` 安静地返回 `-1`，`index()` 直接抛异常。判断「有没有」用 `find()`，确认「一定有」用 `index()`。
- `isdigit()` 配合 `input()` 可以挡住非数字输入（回顾第 05 章的 `ValueError`）：

  ```python
  num = input("请输入一个数字：")
  if num.isdigit():
      print(int(num) * 2)
  else:
      print("输入的不是数字")
  ```

### 16.5 关键：方法不改原字符串

字符串是**不可修改**的。方法只负责「算出并返回一个新字符串」，原变量一个字符都不会变 —— 想让它生效，必须**用变量接住返回值**。

```python
s = "Hello Python"
s.upper()            # 返回值被丢掉了，s 一点没变
print(s)             # Hello Python

s = s.upper()        # ✅ 正确写法：用变量接住
print(s)             # HELLO PYTHON
```

> 这条和第 15 章的「空字符串是 `False`」一样，属于必须记牢的基础结论。

### 16.6 易错点小结

| 易错点 | 正确做法 |
| --- | --- |
| `s.upper()` 单独写一行 | 没接住返回值等于白写，要写 `s = s.upper()` |
| `s.upper` 漏掉括号 | 拿到的是方法本身，不是结果，必须加 `()` |
| `find()` 和 `index()` 混用 | 判断有没有用 `find()`（返回 -1），确认一定有才用 `index()` |
| 以为 `split()` 返回字符串 | 返回的是**列表**，后面要按列表的方式处理（索引、遍历） |
| 以为 `strip('*')` 能去中间的 `*` | 只去**两端**，中间的动不了 |
| 切片和索引搞混 | 切片越界不报错（自动截断），索引越界报 `IndexError` |

---

## 17 数据容器：列表 `list`

> 前面几章处理的都是「一个值」，列表用来装「一堆值」。

### 17.1 定义与四个特点

```python
nums = [80, 60, 80, 90, 56, '元素1', "元素2"]
```

- 用**方括号 `[]`** 定义，元素之间用**逗号**隔开。

| 特点 | 说明 |
| --- | --- |
| 可以存不同类型 | 数字和字符串可以混着放（`80` 和 `'元素1'` 在同一个列表里） |
| 有序 | 每个元素都有固定位置，用下标访问 |
| 可以重复 | `80` 出现两次完全没问题 |
| 可以修改 | 增、删、改都允许 |

> 和字符串最关键的区别：**字符串不可修改，列表可以修改**（回顾 16.5）。

### 17.2 索引与切片

规则和第 16 章**完全一样**：正向从 0 开始，反向从 -1 开始，切片左闭右开。

| 写法 | 含义 | 结果 |
| --- | --- | --- |
| `nums[0]` | 第 1 个元素 | `80` |
| `nums[-7]` | 倒数第 7 个（长度是 7，所以就是第 1 个） | `80` |
| `nums[-1]` | 最后一个元素 | `'元素2'` |
| `nums[0:5:2]` | 从 0 取到 4，步长 2 | `[80, 80, 56]` |
| `nums[100]` | 下标越界 | `IndexError: list index out of range` |

- 切片的结果**还是列表**，不是单个元素。
- 越界的报错文字和字符串不同：列表是 `list index out of range`，字符串是 `string index out of range`。

### 17.3 修改与删除元素

| 操作 | 写法 | 说明 |
| --- | --- | --- |
| 改 | `nums[0] = 90` | 直接给下标赋新值（这是字符串做不到的事） |
| 删 | `del nums[5]` | `del` 是**语句**不是方法，按**索引**删 |

```python
nums = [80, 60, 80, 90, 56, '元素1', "元素2"]
nums[0] = 90        # 把第 1 个元素改成 90
del nums[5]         # 删掉下标 5 的元素
print(nums)         # [90, 60, 80, 90, 56, '元素2']
```

### 17.4 常用方法

| 方法 | 作用 | 示例 | 结果 |
| --- | --- | --- | --- |
| `append()` | 在列表**尾部追加**一个元素 | `nums.append("增加的元素")` | 加到最后面 |
| `insert()` | 在指定索引**之前插入**元素 | `nums.insert(5, "插入的元素")` | 插到下标 5，后面的元素整体后移 |
| `remove()` | 按**值**删除，删掉从头开始**第一个**匹配到的 | `nums.remove("元素1")` | 那个值没了 |
| `pop()` | 按**索引**删除，不写参数就删最后一个；**会把删掉的值返回** | `num = nums.pop(2)` | `num` 拿到 `80` |
| `sort()` | 排序，**只能同类型**数据才能排 | `nums.sort()` | 从小到大排好 |
| `reverse()` | 反转顺序 | `nums.reverse()` | `1 2 3` → `3 2 1` |

```python
# 下面每个方法都从一个全新的 nums 开始，方便单独对照
nums = [80, 60, 80, 90, 56, '元素1', "元素2"]

nums.append("增加的元素")
print(nums)      # [80, 60, 80, 90, 56, '元素1', '元素2', '增加的元素']

nums = [80, 60, 80, 90, 56, '元素1', "元素2"]
nums.insert(5, "插入的元素")
print(nums)      # [80, 60, 80, 90, 56, '插入的元素', '元素1', '元素2']

nums = [80, 60, 80, 90, 56, '元素1', "元素2"]
nums.remove("元素1")
print(nums)      # [80, 60, 80, 90, 56, '元素2']

nums = [80, 60, 80, 90, 56, '元素1', "元素2"]
num = nums.pop(2)
print(num)       # 80
print(nums)      # [80, 60, 90, 56, '元素1', '元素2']
```

**三种删除方式的区别：**

| 写法 | 按什么删 | 有没有返回值 |
| --- | --- | --- |
| `nums.remove(值)` | 按**值** | 没有 |
| `nums.pop(索引)` | 按**索引** | **有**，返回被删的那个值 |
| `del nums[索引]` | 按**索引** | 没有（它是一条语句，不是方法） |

- `append()`、`sort()`、`reverse()` 都是**直接改原列表**，并且**返回 `None`**：

  ```python
  result = nums.sort()
  print(result)      # None —— 千万别写成 nums = nums.sort()
  ```

### 17.5 列表推导式

作用：**一行**按规则生成一个新列表，用来替代「建空列表 + for 循环 append」的啰嗦写法。

| 语法 | 写法 |
| --- | --- |
| 格式一 | `[要插入的值 for i in 序列]` |
| 格式二 | `[要插入的值 for i in 序列 if 条件]` |

「要插入的值」可以是**表达式**，比如 `i ** 2`。

```python
# 写法一：for + append
nums = []
for i in range(1, 21):
    nums.append(i ** 2)
print(nums)

# 写法二：列表推导式 —— 一行搞定，结果完全一样
nums = [i ** 2 for i in range(1, 21)]
print(nums)      # [1, 4, 9, 16, ... 400]
```

带上条件：

```python
# 只取偶数，再平方
print([i ** 2 for i in range(1, 21) if i % 2 == 0])
# [4, 16, 36, 64, 100, 144, 196, 256, 324, 400]
```

### 17.6 案例与练习

```python
# ① 生成 1~20 的平方
print([i ** 2 for i in range(1, 21)])

# ② 把 1~30 里能被 3 或 5 整除的数提出来，并平方
list1 = list(range(1, 31))        # 等价于手写 1~30 那一长串数字
print([i ** 2 for i in list1 if i % 3 == 0 or i % 5 == 0])
# [9, 25, 36, 81, 100, 144, 225, 324, 400, 441, 576, 625, 729, 900]

# ③ 把列表里的正数提出来，再从小到大排序
list2 = [11, 2, 31, 4, -5, 15, 17, 28, 49, 10, -11, 16, 54, -14, 36, -16, 87, -39]
new_list = [i for i in list2 if i > 0]
new_list.sort()
print(new_list)   # [2, 4, 10, 11, 15, 16, 17, 28, 31, 36, 49, 54, 87]
```

### 17.7 易错点小结

| 易错点 | 正确做法 |
| --- | --- |
| 用 `list` 当变量名 | `list` 是 Python 内置的类型名，拿来当变量会把它遮住。改用 `nums`、`scores` 这类名字 |
| `nums = nums.sort()` | `sort()` 返回 `None`，这么写列表就变成 `None` 了。直接写 `nums.sort()` |
| `remove()` 和 `pop()` 混用 | 按**值**删用 `remove`，按**索引**删用 `pop` |
| `remove()` 一个不存在的值 | 报 `ValueError: list.remove(x): x not in list` |
| 混类型列表调用 `sort()` | 报 `TypeError`，`sort()` 只能排同类型数据 |
| 以为切片会改原列表 | 切片返回的是**新列表**，原列表不受影响 |

---

## 18 字符串综合案例

### 18.1 邮箱格式验证

需求：用户输入邮箱，验证格式（**恰好一个** `@`，**至少一个** `.`），正确输出「邮箱格式正确」，否则输出「邮箱格式错误」。

```python
str_mail = input("请输入您的邮箱")
if str_mail.count("@") == 1 and str_mail.count(".") >= 1:
    print("邮箱格式正确")
else:
    print("邮箱格式错误")
```

- 用到的正是 16.3 的 `count()`：数一数某个字符出现了几次。
- `@` 要**恰好 1 个**，所以用 `== 1`；`.` 要**至少 1 个**，所以用 `>= 1` —— 两个条件写法不同，因为要求本来就不同。

**写法二：用 `in` 运算符。** `in` 判断「子串在不在字符串里」，返回布尔值。

```python
str_mail = input("请输入您的邮箱")
if "@" in str_mail and "." in str_mail:
    print("邮箱格式正确")
else:
    print("邮箱格式错误")
```

| 写法 | 能管住「有两个 `@`」吗 |
| --- | --- |
| `str_mail.count("@") == 1` | **能**（数出来是 2，条件不成立） |
| `"@" in str_mail` | 不能（只判断「有没有」，两个 `@` 也会通过） |

> `in` 判断「**有没有**」，`count()` 判断「**有几个**」。列表也能用 `in`：`80 in nums` 判断列表里有没有 80。

### 18.2 判断回文

回文：正着读和倒着读**一样**的字符串，比如「上海自来水来自海上」。

思路：把字符串**反转**一份，跟原串比较，相等就是回文。反转用 16.2 学的切片 `[::-1]`。

```python
str_wen = input("请输入一个字符串")
str_wen2 = ""
for i in str_wen[::-1]:      # 先反转，再一个字符一个字符取出来
    str_wen2 += i            # += 就是 str_wen2 = str_wen2 + i
if str_wen == str_wen2:
    print("你输入的是回文")
else:
    print("你输入的不是回文")
```

**更简单的一行写法：**

```python
str_wen = input("请输入一个字符串")
if str_wen == str_wen[::-1]:
    print("你输入的是回文")
else:
    print("你输入的不是回文")
```

> 上面那个循环做的其实就是「把字符串反转一遍」，而 `[::-1]` 本来就能直接反转 —— 所以一行就够。

### 18.3 字符串反转后转大写，存进列表

需求：把用户输入的字符串**反转**、每个字符转**大写**，存到**列表**里，再遍历输出。

```python
str_list = input("请输入字符串")
new_list = []
for i in str_list[::-1]:
    new_list.append(i.upper())
for k in new_list:
    print(k)
```

- 这一题把第 17 章的列表、第 16 章的切片反转和 `upper()` 串起来用了。
- 也可以用列表推导式一行写完（见 17.5）：

  ```python
  new_list = [i.upper() for i in str_list[::-1]]
  ```

输入 `abc` → 依次输出 `C` `B` `A`，`new_list` 是 `['C', 'B', 'A']`。
